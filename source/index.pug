extends templates/layout.pug

prepend variables
  -
    const title = 'Simple Starter';
    const description = 'The basic boilerplate to start development';

append variables
  -
    html.classList.push('no-js');

    meta.push({
      name: 'theme-color',
      content: '#ffffff',
    });
  //-
    preloads.push('fonts/font-400.woff2');
    links.push({
      rel: 'icon',
      href: 'favicon.ico',
    });
    links.push({
      rel: 'icon',
      href: 'images/favicons/favicon.svg',
      type: 'image/svg+xml',
    });
    links.push({
      rel: 'apple-touch-icon',
      href: 'images/favicons/apple.png',
    });
    links.push({
      rel: 'manifest',
      href: 'manifest.webmanifest',
    });

block content
  include templates/data.pug
  //- include templates/sprite.pug

  include templates/components/header.pug

  main.main
    div.container
      p There will be something awesome!

  include templates/components/footer.pug

append scripts
  //- script(src="js/main.min.js")
